import { defineConfig } from "vite"
import react from "@vitejs/plugin-react"

// Configuração SIMPLES - apenas React
export default defineConfig({
  plugins: [react()],
  server: {
    port: 3000,
    open: true,
    host: true
  }
  // SEM configuração de CSS - usar padrão do Vite
})
