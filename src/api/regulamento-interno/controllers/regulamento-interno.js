'use strict';

/**
 * regulamento-interno controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::regulamento-interno.regulamento-interno');
